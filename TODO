Tests:
- fix integration test errors for nextflow on transpiled .nf files
- fix integration test errors for snakemake on transpiled .smk files

Python:
- input data feeding for transpiled workflows
- cli command for showing workflow inputs and outputs
- data validation for given input data on a target workflow
- Common Subexpression Elimination in lower.py
- interpolation in bash command of task
- remove trailing empty lines in Bash command
- expression evaluation (e.g. memory / cpu) in the task
  (relying on the target language's support for expressions)
  (we need the input data to pre-compute the values!)
- eta reduction (e.g. \x -> f x -> f)

Rust:
- worflow lexer
- worflow parser
- task parser
- semantic checker
- semantic lowering
- transpile to cwl
